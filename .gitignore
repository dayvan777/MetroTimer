# Репозиторий живёт в папке Downloads, куда постоянно попадают посторонние
# файлы (PDF, картинки, дистрибутивы). Отслеживаем только проект — иначе
# случайный `git add -A` тащит в историю чужие гигабайты.
/*
!/.gitignore
!/MetroTimer/

MetroTimer/build/
MetroTimer/build-ios/
.DS_Store
